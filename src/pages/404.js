import * as React from "react"

const NotFound = () => {
  return (
    <div>
      <p>アクセスされたページは存在しません。</p>
    </div>
  )
}

export default NotFound

export const Head = () => <title>Not found</title>
